/** Backend API (JSON). */
export const API_BASE_URL = 'https://future-fs-01-huwr.onrender.com/api';

/** Origin of the API server (no /api). Used for root-relative media paths stored in the DB. */
export const API_ORIGIN = 'https://future-fs-01-huwr.onrender.com';
